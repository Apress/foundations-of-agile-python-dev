The directory "buildmaster" contains the buildbot master config
for Chapter06. The only changes is the addition of a step to run
unit tests.  The directory is rooted at '/' on build master.

The directory "rsreader" contains the project code developed
in Chapter06.

