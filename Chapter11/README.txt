The directory "buildmaster" contains the a Buildbot master config that runs the PyFIT acceptance tests from the RSReader project.  This version of the RSReader project is in the directory "rsreader".

The directory "rsreader" contains the RSReader project with support for acceptance testing.  It can be imported into Eclipse.


